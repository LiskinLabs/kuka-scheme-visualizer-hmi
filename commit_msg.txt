🧪 [testing improvement] add coverage for loadState

🎯 What: Added unit tests using Node vm sandbox for HmiApp.loadState
📊 Coverage: Added coverage for valid state paths, out-of-range bounds checking, edge case handling (gapH interpolation logic), invalid JSON persistence, non-object rejection, position array validation parsing via isNum, and unknown project protection.
✨ Result: Reliable validation layer added for testing complex local storage state persistence.
